# 运行指南

克隆后先读根目录 `README.md` 的「别人克隆后怎么跑」。下面是补充细节。

## 配置文件

```bash
cp .env.example .env
```

至少确认：

- `MOONSHOT_API_KEY` 已填，**或** `LLM_PROVIDER=ollama` 且本机 Ollama 可用  
- `JWT_SECRET` ≥ 32 位（示例文件已给可用值）  
- `ADMIN_PASSWORD` 不是 `Admin123!`（Docker prod 会拒绝）  
- 第一次 Docker 部署：`ADMIN_RESET_PASSWORD=true`，登录成功后改 `false`

## A. Docker 全栈（推荐）

**不要**只执行 `docker compose up --build`：镜像构建依赖本机先打好：

- `backend/target/*.jar`
- `frontend/dist/`

Windows：双击 `一键部署.bat`（会检查环境、自动打包、再 compose）。

手动等价命令：

```bash
cd backend && mvn -DskipTests package && cd ..
cd frontend && npm ci && npm run build && cd ..
docker compose up -d --build
```

访问 http://localhost ，健康检查 http://localhost/api/health  

期望：`mysql=UP`、`qdrant=UP`；Ollama 正常时 `embed=ollama`。

管理员：`.env` 的 `ADMIN_EMAIL` / `ADMIN_PASSWORD`。

语义向量建议：

```bash
ollama pull nomic-embed-text
```

Windows 可先双击 `启动Ollama.bat`（监听 `0.0.0.0:11434`，供 Docker 通过 `host.docker.internal` 访问）。

## B. 本机开发

1. `初始化数据库.bat` 只启动 MySQL（3307）  
2. `启动后端.bat` / `mvn spring-boot:run`（不要 `SPRING_PROFILES_ACTIVE=prod`）  
3. `启动前端.bat` → http://localhost:5173  

可不启 Qdrant / Redis；检索走内存余弦，限流走进程内计数。

## 建议测试问题

- 专业版多少钱？
- 忘记密码怎么办？
- 订阅 7 天内可以退款吗？
- 今天股市怎么样？（应兜底）
