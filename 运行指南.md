# 运行指南

克隆仓库后，在**项目根目录**复制 `.env.example` 为 `.env` 再改配置。下列脚本使用相对路径，在仓库根目录双击即可。

## 第一次（数据库）

若本机 3306 已被占用，可用 Docker 在 **3307** 起独立库：

- 库名 `ai_cs`
- 用户 `root`
- 密码 `ai_cs_dev`（写在本地 `.env`，不要提交 Git）

1. 打开 **Docker Desktop**，等状态变为 Running  
2. 在仓库根目录双击 `初始化数据库.bat`

容器首次启动会执行 `backend/数据库初始化脚本/init.sql` 建库建表。后端启动时 Flyway 会补齐迁移（若已手工执行完整 `init.sql`，见该目录 `说明.md`）。

管理员与示例知识库由应用启动时写入（`admin@company.com` / `Admin123!`）。

## 后端

不需要 IDEA。在仓库根目录双击 `启动后端.bat`（会读取 `.env`）。  
成功标志：终端出现端口 **8080**。

命令行：

```bash
cd backend
mvn spring-boot:run
```

## 前端

在仓库根目录双击 `启动前端.bat`，浏览器打开 http://localhost:5173  

或双击 `一键启动.bat`。

```bash
cd frontend
npm install
npm run dev
```

`/api` 已代理到 `http://localhost:8080`。

## 大模型（问答）

默认走 **月之暗面云 API**。在根目录 `.env` 配置 `MOONSHOT_API_KEY`（不要提交 Git）。

改用本机 Ollama 对话：`.env` 中 `LLM_PROVIDER=ollama`，并运行 `启动Ollama.bat`。

详见 `backend/README.md`。

## 向量（检索）

`EMBED_PROVIDER=auto`（推荐）：启动时探测本机 Ollama，能通则用 **`nomic-embed-text`**。请先：

```bat
ollama pull nomic-embed-text
```

并保持 Ollama 在跑。不可用时自动改用本机词法向量（功能可跑，近义检索会变弱）。

向量维数变化时后端会自动重嵌已有文档，第一次可能多等几十秒。

## 建议测试问题

- 专业版多少钱？（产品分区）
- 忘记密码怎么办？
- 订阅 7 天内可以退款吗？（售后分区）
- 今天股市怎么样？（超出范围 / 兜底）
- 答完后点气泡下的追问芯片
