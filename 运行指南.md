# 运行指南

克隆后在项目根目录复制 `.env.example` 为 `.env`，填入密钥。**不要把 `.env` 提交到 Git。**

有两种跑法：开发机分前后端，或 Docker 一键部署（推荐上线形态）。

## A. Docker 一键部署（推荐）

需要 Docker Desktop。`.env` 里至少：

- `JWT_SECRET`：≥32 位，不要包含 `please-change` / `change-me` / `example`
- `ADMIN_PASSWORD`：不要再用 `Admin123!`（首次可用 `ADMIN_RESET_PASSWORD=true` 写入）
- `MOONSHOT_API_KEY`（若 `LLM_PROVIDER=moonshot`）
- 语义向量：本机先跑 `启动Ollama.bat` 并 `ollama pull nomic-embed-text`（Compose 后端通过 `host.docker.internal` 访问）；或配置 `EMBED_API_KEY`

Windows 可双击 `一键部署.bat`，或：

```bash
docker compose up -d --build
```

浏览器打开 http://localhost  
健康检查：http://localhost/api/health  

服务组成：Nginx 前端（80）→ Spring Boot（8080）→ MySQL 8、Qdrant、Redis。

管理员账号为 `.env` 中的 `ADMIN_EMAIL` / `ADMIN_PASSWORD`。已有库时**不会**每次启动重置密码，除非 `ADMIN_RESET_PASSWORD=true`。重置成功后请把 `ADMIN_RESET_PASSWORD` 改回 `false`。

## B. 本机开发（前后端分离）

只起数据库：双击 `初始化数据库.bat`（仅 MySQL **3307**）。

后端：`启动后端.bat` 或 `cd backend && mvn spring-boot:run`（不要开 `prod` profile）。  
前端：`启动前端.bat`，打开 http://localhost:5173

开发模式可不配 Qdrant：检索走 MySQL 向量 + 内存余弦。Compose 部署时由 `QDRANT_URL` 启用 Qdrant，挂了会自动回退内存索引。

本地默认管理员：`.env` 未改时仍为 `admin@company.com` / `Admin123!`。

## 模型与向量

| 用途 | 配置 |
|---|---|
| 对话 | `LLM_PROVIDER=moonshot` + `MOONSHOT_API_KEY`，或 `ollama` |
| Embedding | `EMBED_PROVIDER=auto`：先试 `EMBED_API_KEY` 的 OpenAI 兼容接口，再试 Ollama `nomic-embed-text`，最后本机词法向量 |

```bat
ollama pull nomic-embed-text
```

## 建议测试问题

- 专业版多少钱？
- 忘记密码怎么办？
- 订阅 7 天内可以退款吗？
- 今天股市怎么样？（应走兜底，不编造）
