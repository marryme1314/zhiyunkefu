# 运行指南

克隆仓库后，在项目根目录复制 `.env.example` 为 `.env` 再改配置。下列脚本已改为相对路径，不依赖固定盘符。

## 第一次（数据库）

若本机 3306 已被占用，可用 Docker 在 **3307** 起独立库：

- 库名 `ai_cs`
- 用户 `root`
- 密码 `ai_cs_dev`（本地 `.env`，不要提交 Git）

1. 从开始菜单打开 **Docker Desktop**，等左下角变成 Running
2. 双击 `E:\bishi\初始化数据库.bat`

容器首次启动会执行 `backend/数据库初始化脚本/init.sql` 建好全部表。

## 后端

不需要 IDEA。双击 `E:\bishi\启动后端.bat`（会自动读取 `.env`）。  
成功标志：终端出现端口 **8080**。

## 前端

前端已写在 `E:\bishi\frontend`。双击 `E:\bishi\启动前端.bat`，浏览器打开 http://localhost:5173

或双击 `E:\bishi\一键启动.bat`。

## 大模型（问答）

当前默认走 **月之暗面云 API**。在 `E:\bishi\.env` 里配置 `MOONSHOT_API_KEY`（不要提交 Git）。

本机 Ollama 改为对话模型：把 `.env` 里 `LLM_PROVIDER` 改成 `ollama`，并双击 `启动Ollama.bat`。

## 向量（检索）

`EMBED_PROVIDER=auto`（推荐）：启动时探测本机 Ollama，能通则用 **`nomic-embed-text`** 语义向量。请先：

```bat
ollama pull nomic-embed-text
```

并保持 `启动Ollama.bat` 在跑。Ollama 不可用时自动改用本机词法向量，功能仍可用，近义改写检索会变弱。

向量维数变化时后端会自动重嵌已有文档，第一次可能多等几十秒。

## 建议测试问题

- 专业版多少钱？（产品分区）
- 忘记密码怎么办？
- 订阅 7 天内可以退款吗？（售后分区）
- 今天股市怎么样？（超出范围 / 兜底）
- 答完后点气泡下的追问芯片
