# 前端

Vue 3 + TypeScript + Vite。

```bash
cd frontend
npm install
npm run dev
```

开发地址：http://localhost:5173  
`/api` 代理到后端 `http://localhost:8080`。

构建：

```bash
npm run build
```

Windows 也可双击根目录 `启动前端.bat`。
