# 智云客服

基于 **Java Spring Boot 3 + Vue 3 + MySQL** 的企业知识库智能客服。对话可用 **月之暗面 API** 或本机 **Ollama**；检索优先 **Ollama nomic-embed-text**。

## 技术选型及原因

| 层 | 选择 | 原因 |
|---|---|---|
| 后端 | Spring Boot 3 / Java 17 | 企业后端常用栈；SSE、JPA、Security 开箱即用 |
| 前端 | Vue 3 + TypeScript + Vite | 题目二选一 |
| 对话 LLM | Moonshot `kimi-k2.6`（可切 Ollama qwen2） | 云端稳定；本机 GPU 不可用时仍能演示 |
| Embedding | 默认 Ollama `nomic-embed-text`；不可用则本机词法向量 | 题目要求语义向量；Moonshot Embedding 多数 Key 未开通（403） |
| 向量存储 | MySQL `embedding_json` + 内存余弦 | 语料小（数千字），避免 Windows 上再部署向量服务；**不是**独立 Faiss/Chroma 进程 |
| 关系库 | MySQL 8 | 题目指定 |

未使用 LangChain：切分、向量化、Top-K、拼 Prompt、流式调用均在业务代码中显式实现，便于说明每一步原理。

## 使用的 AI 工具（题目要求如实说明）

| 类型 | 工具 | 用途 |
|---|---|---|
| 编程助手 | Cursor | 辅助生成 Spring Boot / Vue 样板、接口与页面骨架，再由人工审阅、改错与补齐业务约束 |
| 对话 LLM | 月之暗面 Moonshot（`kimi-k2.6`） | 客服回答流式生成；意图分类、追问建议的短补全 |
| 备用对话 | 本机 Ollama（`qwen2`） | 无云端 Key 或云端不可用时的对话回退 |
| Embedding | Ollama `nomic-embed-text` | 文档切片与问题向量化（优先） |
| Embedding 回退 | 本机词法哈希向量 | Moonshot Embedding 未开通、且 Ollama 未启动时保证链路可跑 |

未把真实 API Key 写入仓库；仅通过本地 `.env`（已 gitignore）与 `.env.example` 模板配置。

## 构建 RAG 时对 AI 生成代码做的优化与修正

AI 助手给出的初版常见问题是「能调通接口」，但缺少客服场景约束。人工重点改了这些：

1. **空检索短路**：检索无命中时**不调用**对话模型，直接返回配置兜底话术，避免模型编造套餐/退款政策。
2. **相似度阈值 + 短问兜底**：余弦低于阈值视为未命中；极短关键词（如「退款」）增加文本包含兜底，减少漏召回。
3. **SSE 与鉴权**：不用浏览器 `EventSource`（无法方便带 JWT），改用 `fetch` 读流；后端事件名固定为 `meta` / `token` / `done` / `error`，并先推送引用来源再推 token。
4. **上下文截断**：只携带最近 N 轮（默认 3）历史，防止把整段会话塞进 Prompt 导致超长或串题。
5. **System Prompt 约束**：要求只依据检索结果作答；资料不足时明确说明，禁止臆造流程与价格。
6. **Embedding 维度一致性**：进程内锁定一种 Embedding 后端；库中向量维数变化时启动自动重嵌，避免 384/768 维混算余弦。
7. **流式中断与落库**：用户停止生成时中断 SSE，已生成内容仍可落库并标记中断，避免半截回答丢失。
8. **意图与追问降级**：意图/追问优先走短超时 LLM JSON；解析失败回退规则/启发式，保证演示不因分类超时而整链失败。

## 如何评估与验证 RAG 回答质量

以**人工场景测试**为主（语料规模小，适合可复现的手工验收），并结合界面与后台核对：

| 用例类型 | 示例提问 | 期望 |
|---|---|---|
| 命中原句/近义 | 「专业版多少钱？」「订阅 7 天内可以退款吗？」 | 回答依据种子文档；气泡「参考资料」含正确文档名与片段 |
| 账号类 FAQ | 「忘记密码怎么办？」 | 命中 FAQ 分区相关切片，步骤与文档一致 |
| 空检索 / 超范围 | 「今天股市怎么样？」 | 走超出范围或空检索兜底，**不编造**行情或内部政策 |
| 闲聊 / 转人工 | 「你好」「转人工」 | 不走完整 RAG 编造；返回固定引导话术 |
| 多轮指代 | 先问套餐，再问「那企业版呢？」 | 在历史轮次内能承接指代（依赖最近 N 轮） |
| 流式与引用 | 任意命中问题 | 先见 `meta` 来源，再逐字输出；刷新会话后引用仍可从历史消息还原 |

额外核对：

- 管理后台意图分布、反馈有用/无用是否随对话变化；
- 删除知识库文档后，相关提问是否更容易落入空检索；
- Ollama 未启动时系统是否回退词法向量且仍可问答（质量下降可接受，链路不断）。

未做大规模自动评测集；对本题语料规模，上述用例足以验证「能检索、能引用、空库不胡说」。

## 扩展能力（加分项）

| 项 | 状态 |
|---|---|
| 意图识别 | LLM JSON 分类 + 关键词兜底；写入 `messages.intent` |
| 追问建议 | 回答结束后最多 3 条可点击追问（LLM，失败则启发式） |
| 管理后台 | KPI、趋势、意图分布、全量会话、用户级联删除 |
| 多知识库路由 | 文档分区 PRODUCT / AFTER_SALES / FAQ / GENERAL，按意图优先检索，未命中再全库 |

## 业务问题处理（对应 AI 工程思维）

- **检索为空**：不生成政策，走兜底或投诉安抚话术。
- **上下文超长**：只带最近 N 轮（默认 3）历史。
- **幻觉**：System Prompt 只依据检索结果；空检索短路；回答展示引用来源便于人工核对。
- **意图失败**：LLM 超时/非 JSON 时回退规则，保证演示不中断。
- **模型/向量服务异常**：对话与 Embedding 均有回退路径，接口返回可读错误而非裸堆栈。
