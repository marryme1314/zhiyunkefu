# 智云客服

基于 **Java Spring Boot 3 + Vue 3 + MySQL** 的企业知识库智能客服。对话可用 **月之暗面 API** 或本机 **Ollama**；检索优先 **Ollama nomic-embed-text**。

## 技术选型及原因

| 层 | 选择 | 原因 |
|---|---|---|
| 后端 | Spring Boot 3 / Java 17 | 企业后端常用栈；SSE、JPA、Security 开箱即用 |
| 前端 | Vue 3 + TypeScript + Vite | 题目二选一 |
| 对话 LLM | Moonshot `kimi-k2.6`（可切 Ollama qwen2） | 云端稳定；本机 GPU 不可用时仍能演示 |
| Embedding | 默认 Ollama `nomic-embed-text`；不可用则本机词法向量 | 题目要求语义向量；Moonshot Embedding 多数 Key 未开通（403） |
| 向量存储 | MySQL `embedding_json` + 内存余弦 | 语料小（数千字），避免 Windows 上再部署向量服务；**不是** Faiss/Chroma 文件引擎 |
| 关系库 | MySQL 8 | 题目指定 |

未使用 LangChain：切分、向量化、Top-K、拼 Prompt、流式调用均在业务代码中显式实现。

## 加分项范围

| 项 | 状态 |
|---|---|
| 意图识别 | 已做：LLM JSON 分类 + 关键词兜底；写入 `messages.intent` |
| 追问建议 | 已做：回答结束后 3 条可点击追问（LLM，失败则启发式） |
| 管理后台 | 已做：KPI、趋势、意图分布、全量会话、用户级联删除 |
| 多知识库路由 | 已做：文档分区 PRODUCT / AFTER_SALES / FAQ / GENERAL，按意图优先检索，未命中再全库 |

## 模型与检索

- 对话：Moonshot / Ollama
- Embedding：Ollama nomic-embed-text（优先）

实现时主动做的约束：

1. 空检索**不调用**对话模型，返回配置兜底，降低幻觉。
2. 检索带相似度阈值；短问另有关键词包含兜底。
3. SSE 具名事件 `meta` / `token` / `done` / `error`；前端用 `fetch` 带 JWT（不用 EventSource）。
4. 日限额、500 字、历史轮次截断在后端强制。
5. Embedding 后端在进程内锁定，避免 384 维与 768 维向量混用；维度变化时启动重嵌。

## 业务问题处理

- **检索为空**：不生成政策，走兜底或投诉安抚话术。
- **上下文超长**：只带最近 N 轮（默认 3）历史。
- **幻觉**：System Prompt 要求只依据检索结果；空检索短路；回答展示引用来源。
- **意图失败**：LLM 超时/非 JSON 时回退规则，保证演示不中断。
